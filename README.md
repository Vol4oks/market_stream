# market_stream
Это учебный проект 
#

## Система клиент-серверного взаимодействия для стриминга биржевых данных
### Команды для запуска 
- Сервер `$ cargo run --bin server` 
- Клиент `$ cargo run --bin client -- --tickers-path ./t_client.txt` 

